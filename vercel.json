{
  "framework": "vite",
  "installCommand": "rm -f package-lock.json bun.lock && node scripts/strip-workspace-deps.js && bun install --no-save && node scripts/create-shims.js",
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
