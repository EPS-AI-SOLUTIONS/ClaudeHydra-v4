{
  "framework": "vite",
  "installCommand": "rm -f package-lock.json bun.lock && bun scripts/strip-workspace-deps.ts && bun install --no-save && bun scripts/create-shims.ts",
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "regions": ["cdg1", "fra1", "iad1"],
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . ':!docs' ':!*.md' ':!scripts' ':!.claude'",
  "rewrites": [
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
