# ClaudeHydra v4 — Staging deployment config
# Usage: fly deploy --config fly.staging.toml
#
# Secrets: fly secrets set DATABASE_URL=... --app claudehydra-v4-staging

app = 'claudehydra-v4-staging'
primary_region = 'arn'

[build]

[deploy]
  strategy = "rolling"

[http_service]
  internal_port = 8082
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0

  [http_service.checks]
    [http_service.checks.health]
      interval = "30s"
      timeout = "5s"
      grace_period = "10s"
      method = "GET"
      path = "/api/health"

[env]
  RUST_LOG = "debug"
  RUST_LOG_FORMAT = "json"

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
